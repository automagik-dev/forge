# Forge Overrides

This directory will collect thin glue code or configuration files that adapt upstream components without modifying the upstream sources themselves.

For Task 1 the directory is intentionally empty—keep future overrides small, well-documented, and isolated from the upstream submodule.
