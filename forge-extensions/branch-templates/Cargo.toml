[package]
name = "forge-branch-templates"
version = "0.1.0"
edition = "2021"

[dependencies]
upstream-db = { path = "../../upstream/crates/db", package = "db" }
sqlx = { version = "0.8.6", features = [ "runtime-tokio-rustls", "sqlite" ] }
